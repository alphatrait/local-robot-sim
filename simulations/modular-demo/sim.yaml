version: 2

spawn:
  position: [0, 0.05, 0]
  rotation: [0, 0, 0, 1]

controller:
  path: controllers/main.py
  entrypoint: main
  loop_hz: 60
  max_step_ms: 8
  max_violations: 3

simulation:
  physics_hz: 60

reload:
  policy: soft_reset

sync:
  mode: local
  poll_interval_sec: 15
